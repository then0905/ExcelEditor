# JsonEditor 打包與發布說明

## 1. 打包

雙擊專案根目錄的 **`JsonEditor.bat`** 即可。它會:

1. 用 Nuitka 打包 → 產出到 `nuitka_dist\main.dist\`
2. 用 `robocopy` 把成果覆蓋部署到 `C:\Users\USER\Desktop\JsonEditor\JsonEditor`

打包指令(`JsonEditor.bat` 內容):

```bat
.venv\Scripts\python.exe -m nuitka --standalone --windows-console-mode=disable ^
  --enable-plugin=pyside6 --windows-icon-from-ico=icon.ico ^
  --include-data-files=icon.ico=icon.ico ^
  --experimental=force-dependencies-pefile ^
  --output-dir=nuitka_dist --output-filename=JsonEditor ^
  --assume-yes-for-downloads main.py
```

### 為什麼一定要 `--experimental=force-dependencies-pefile`

專案實體路徑含中文「工具」(`D:\HLO\excel工具\SkillExcelEditor`)。
Nuitka 預設用 `depends.exe` 偵測 DLL 依賴,它以 cp950 輸出路徑、Nuitka 卻用 latin1 解碼
→ 中文路徑變亂碼 → `AssertionError` 崩潰。

加上 `--experimental=force-dependencies-pefile` 改用純 Python 的 `pefile`,能正確處理 Unicode 路徑。

> `D:\JEB` 是指向專案的 symlink,**無法**繞過此問題 —— Windows 解析依賴時會還原成真實中文路徑。
> 若要徹底擺脫,得把專案搬到純 ASCII 路徑並重建 venv。

---

## 2. 打包產物

Nuitka `--standalone` 產出的是**整個資料夾** `nuitka_dist\main.dist\`(約 739 個檔、201 MB):

- `JsonEditor.exe`
- 數百個 `.dll` / `.pyd`
- `PySide6\`、`numpy\`、`pandas\`、`pytz\` … 等資料夾

⚠ **`JsonEditor.exe` 不能單獨抽出來執行**,整個資料夾才是完整程式。

### onefile(可選,單一 exe)

若覺得檔案太多難管理,可改用單一檔模式 —— 在打包指令加上 `--onefile`,
產出只有一個 `JsonEditor.exe`(自解壓,約 80–90 MB)。

| | standalone 資料夾(目前) | onefile 單檔 |
|--|--|--|
| 檔案數 | 739 檔 | 1 檔 |
| 蓋檔 / 發布 | 要整包處理 | 換一個檔即可 |
| 啟動速度 | 快 | 稍慢(每次解壓到 temp) |
| 防毒 / SmartScreen | 較不易誤判 | 自解壓較易誤判 |

---

## 3. 手動蓋檔(部署)

`JsonEditor.bat` 已用 `robocopy /MIR /XF config.json` 自動部署。若要**手動**做:

1. 關掉目標端正在執行的 `JsonEditor.exe`
2. 進目標資料夾 `C:\Users\USER\Desktop\JsonEditor\JsonEditor`,把 `config.json` 複製出來備份(那是設定檔)
3. 刪掉整個舊資料夾內容
4. 把 `nuitka_dist\main.dist` 內**全部**檔案複製過去
5. 把 `config.json` 放回去

重點:**整個資料夾當一個單位處理**,不要逐檔挑。

---

## 4. GitHub 發布版本

Repo:`https://github.com/then0905/JsonEditor`
目前版號:`v1.0.6` → 下一版 `v1.0.7`

### 步驟(網頁,最簡單)

1. 先把程式碼改動 **commit + push**(release 的 tag 才會指到正確版本)
2. 開 `https://github.com/then0905/JsonEditor/releases` → **Draft a new release**
3. **Choose a tag** → 輸入 `v1.0.7` → 選 **Create new tag on publish**
4. 填標題(`v1.0.7`)與說明(這版改了什麼)
5. **Attach binaries** 區域拖入打包產物:
   - standalone 資料夾 → 先把 `main.dist` 壓成 `JsonEditor.zip` 再拖(GitHub 只能傳檔案,不能傳資料夾)
   - onefile → 直接拖 `JsonEditor.exe`
6. **Publish release**

### 步驟(gh CLI,可選)

`gh` 未安裝,需先 `winget install GitHub.cli`,之後:

```bat
gh release create v1.0.7 JsonEditor.exe --title "v1.0.7" --notes "修正內容說明"
```

---

## 5. 常見問題

- **打包失敗,`PermissionError` / `_internal` 檔案被鎖**
  → 目標端的 `JsonEditor.exe` 還在執行,先關掉再打包。

- **`build_nuitka.bat` 崩潰、出現亂碼路徑 `excel\x04u\x04\x04`**
  → 沒加 `--experimental=force-dependencies-pefile`,見第 1 節。

- **`.bat` 雙擊報錯「( 此時不應有 )」之類**
  → `.bat` 檔被存成 LF 換行;Windows 批次檔必須是 CRLF。
